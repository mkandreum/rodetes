# Base URL for the API. In local development with Vite proxy, this might not be needed if relying on relative paths /api.
# If using a separate backend URL in production:
# VITE_API_BASE_URL=http://localhost:3000

# Note: The current setup uses vite.config.ts proxying for /api -> localhost:3000
